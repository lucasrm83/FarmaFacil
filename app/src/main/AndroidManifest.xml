<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lucas.farmafcil">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".BuscaFuncActivity"></activity>
        <activity android:name=".AtualizandoMedicamentoActivity" />
        <activity android:name=".MedicamentoFuncActivity" />
        <activity android:name=".MedicamentoCadastradoActivity" />
        <activity android:name=".LoginFuncActivity" />
        <activity android:name=".ListaDeEsperaActivity" />
        <activity android:name=".MedicamentoExcluidoActivity" />
        <activity android:name=".MedicamentoAtualizadoActivity" />
        <activity android:name=".MedicamentoActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".CadastroMedicamentoActivity" />
        <activity android:name=".CadastroPessoaFimActivity" />
        <activity android:name=".MenuFuncActivity" />
        <activity android:name=".CadastroPessoaActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".BuscaActivity" />
        <activity
            android:name=".InicialActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>